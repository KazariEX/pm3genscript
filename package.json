{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --filter \"!@pm3genscript/language-server\" --parallel dev",
    "release": "bumpp --no-push -c \"release: v%s\"",
    "test": "vitest"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@zinkawaii/eslint-config": "catalog:",
    "@zinkawaii/tsconfig": "catalog:",
    "bumpp": "catalog:",
    "eslint": "catalog:",
    "tsdown": "catalog:",
    "vitest": "catalog:"
  }
}